import styled from "styled-components";

const MainPage = () => {
  return (
    <Container>
      <div>test</div>
    </Container>
  );
};

export default MainPage;

const Container = styled.main`
  display: block;
  font-size: 16px;
  margin: auto;
  max-width: 1024px;
`;
